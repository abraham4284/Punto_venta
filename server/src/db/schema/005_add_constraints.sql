/*
  005_add_constraints.sql
  Generated from server/src/db/export_db structure-only dump.
  Target defaults: ENGINE=InnoDB, CHARACTER SET utf8mb4, COLLATE utf8mb4_unicode_ci.
*/

USE `punto_venta_dev_clean`;

/*
  No additional CHECK constraints are added here.
  The current dump expresses domain restrictions with NOT NULL, DEFAULT, ENUM,
  PRIMARY KEY, UNIQUE KEY and FOREIGN KEY definitions.
*/
